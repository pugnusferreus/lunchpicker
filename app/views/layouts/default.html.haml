<!DOCTYPE html>
%html

  %head
    %meta{ "http-equiv" => "Content-type", :content => "text/html; charset=utf-8" }
    %title
      Lunch Picker
    = stylesheet_link_tag 'style'
    
    %script{ :src => "http://maps.google.com/maps?file=api&amp;v=2&amp;sensor=false&amp;key=#{$GOOGLE_MAP_KEY}", :type => "text/javascript" }
    = javascript_include_tag 'google_map'
    :javascript
      function initialize()
      {
        initMap('#{@search.venue_address}', 'map_canvas');
      }
      
  %body{ :onload => "initialize()" }
    #container

      #header_menu
        = link_to 'Lunch Picker'
      #main      
        %p{ :style => "color: green" }
        = flash[:notice]
      
        #sidebar
          %h3
            Maintenance
    
          %p
            = link_to 'Venues', :controller => 'venues', :action => 'index'
          %p
            = link_to 'Locations', :controller => 'locations', :action=> 'index'
        #text
          = yield
        
      #footer_menu
        Copyright 2009 pugnusferreus.spawncamp.net  